# ─────────────────── Redis ───────────────────
REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=
REDIS_DB=

# ─────────────────── PostgreSQL ──────────────
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# ─────────────────── App ──────────────WW───────
# Rate-limit: max votes per user within the window
RATE_LIMIT_MAX_VOTES=
# Sliding-window size in seconds
RATE_LIMIT_WINDOW_SECONDS=
# How long (seconds) a candidate's vote-count is cached in Redis
CACHE_TTL_SECONDS=
